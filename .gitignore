# Content mount — per-firm client work, credentials context, run artifacts. Never publishes.
local/

# Tooling set — enumerated in harness/checks/tooling-ignore.allowed with reasons.
__pycache__/
.sf/
.sfdx/
node_modules/
.venv/

# Retrieved documentation cache — built locally by scripts/kb-fetch.py, never committed.
# Raw vendor docs are not redistributable and are not usable at this granularity anyway;
# what ships is the DIGESTED catalogue in knowledge/salesforce-platform.yml.
knowledge/cache/
